package tools;

/**
 * Enum to represent different assignment operations
 */
public enum Operations {
    AND,
    ALLOCATE,
    ARRAYALLOCATE,
    ARRAYASSIGN,
    STORE,
    LEQ,
    ADD,
    SUB,
    MULT,
    ARRAYLOOKUP,
    ARRAYLENGTH,
    LOAD,
    FCALL,
    ASSIGNCONST,
    ASSIGN,
    DOUBLEASSIGN,
    NOT,
    NONE
}
