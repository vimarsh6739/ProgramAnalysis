package tools;

public enum Operations {
    NOP,
    BLOCK,
    AND,LT,ADD,SUB,MULT,FIELDREAD,FIELDASSIGN,
    IF_ELSE,
    WHILE,
    SYNC,
    PRINT,
    START, JOIN, WAIT,NOTIFY,NOTIFYALL, 
    WAIT_PRED, NOTIFY_ENTRY
}
